name: run_composite_portfolio
version: "1.0"

description: |
  Construct and evaluate a composite portfolio from multiple strategy runs.
  Combines individual runs into a unified portfolio: data prep → portfolio evaluator → capital wrapper → optimal profile selection → robustness evaluation.

entrypoint:
  type: workflow
  file: prompt.md

properties:
  side_effects: filesystem_write
