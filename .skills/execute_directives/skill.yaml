name: execute_directives
version: "1.0"

description: |
  Execute all active directives through the governed pipeline with Admission Gate enforcement.
  The Golden Path: Admission Gate → Provision-Only Validation → Strategy Review → Warmup Test → Full Pipeline → Capital Wrapper → Candidate Promotion.

entrypoint:
  type: workflow
  file: prompt.md

properties:
  side_effects: pipeline_mutation
