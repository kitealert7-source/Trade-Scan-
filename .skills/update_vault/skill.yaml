name: update_vault
version: "1.0"

description: |
  Create a vault snapshot of the current workspace state.
  Follows ENGINE_VAULT_CONTRACT.md (§4A, §11): verify clean state → preflight → create workspace snapshot → verify manifest → report.

entrypoint:
  type: workflow
  file: prompt.md

properties:
  side_effects: filesystem_write
