name: system_maintenance
version: "1.0"

description: |
  Perform safe system or repository maintenance using governance tools.
  Unified Operational Hub: system health audit, state reset, pipeline smoke tests, vault snapshots, Excel formatting, legacy migration utilities.

entrypoint:
  type: workflow
  file: prompt.md

properties:
  side_effects: filesystem_write
