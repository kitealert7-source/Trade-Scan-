test:
  name: 65_BRK_XAUUSD_5M_PSBRK_S02_V1_P01
  family: BRK
  strategy: 65_BRK_XAUUSD_5M_PSBRK_S02_V1_P01
  version: 1
  signal_version: 2
  broker: OctaFx
  timeframe: 5m
  start_date: "2025-09-01"
  end_date: "2026-05-04"
  research_mode: true
  tuning_allowed: false
  parameter_mutation: false
  description: "Prior Session Liquidity Breakout — NEW LINEAGE (S02) under universal exchange-session ontology (5M). S02_V1_P01 inherits S01_V4_P14 logic byte-for-byte (armed-once-per-session-per-direction guard, structural session-extreme stop, +3R TP, lock+1R after +2R MFE intrabar, utc_day_close fallback) and changes ONE thing: the session ontology. S01 used XAU-liquidity-tuned UTC sessions (asia 00-07 / london 07-13 / ny 13-21). S02 uses canonical exchange-aligned UTC sessions (asia 00-08 / london 08-14 / ny 14-21) with the same 21-24 dead zone. Goal: determine whether PSBRK edge derives from prior-session-extreme breakout structure (portable) or from XAU-tuned timing (asset-specific). Outcome decides whether S02 becomes the portable baseline for cross-asset tests (EURUSD, USDJPY, XAGUSD)."
  notes: "Lineage is S02 (new sweep series), V1 (first version), P01 (first pass). The lineage break is intentional — changing session boundaries shifts breakout levels, stop placement, prev-session highs/lows, and the entire trade population, so this is treated as a new strategy not a patch. ONLY behavioural delta vs S01_V4_P14: indicators[0] points to indicators.structure.session_clock_universal instead of indicators.structure.session_clock. The new indicator hardcodes the universal exchange-session boundaries (no session inputs, no parameters, no per-asset tuning). prev_session_extremes is reused unchanged — it is session-agnostic at the boundary level (consumes session_id/session_seq/session_high_running emitted by whatever session_clock variant is supplied). All other settings preserved: long+short, 0.01 lots, structural stop, +3R TP, lock+1R@2R, utc_day_close, no filters, same engine v1.5.8, same broker (OctaFx). DATE RANGE: P14 directive used 2024-07-19..2026-05-04 but actual XAU 5m research data starts 2025-08-21. After NAS restore, S02 uses 2025-09-01..2026-05-04 (~8 months) — comparison vs P14 will be window-shifted but on identical canonical data. New SIGNATURE_HASH (regenerated by new_pass.py --rehash) is distinct from S01's 6ee99532c5506205 because STRATEGY_SIGNATURE.indicators[0] differs."

symbols:
  - XAUUSD

indicators:
  - indicators.structure.session_clock_universal
  - indicators.structure.prev_session_extremes
  - indicators.volatility.atr

execution_rules:
  pyramiding: false
  entry_when_flat_only: true
  reset_on_exit: true
  stop_loss:
    type: structural_session_extreme
  trailing_stop:
    enabled: false
  take_profit:
    enabled: false
  entry_logic:
    type: prior_session_extreme_breakout
  exit_logic:
    type: stop_or_time

exit_rules:
  type: time_based
  timing: utc_day_close
  target: none

order_placement:
  type: market
  execution_timing: next_bar_open

position_management:
  lots: 0.01

trade_management:
  direction_restriction: long_and_short
  reentry:
    allowed: false

state_machine:
  entry:
    trigger: signal_bar
    direction: long_and_short
  no_reentry_after_stop: true
  session_reset: utc_day
