AGENT PROMPT — STRATEGY TESTING EXECUTION (v2.3)

OBJECTIVE
Execute all directives deterministically via the pipeline manager.
No stage skipping. No inference. No mutation.

Follow SOP_Testing, SOP_Output and other SOPs strictly.
No mutation of prior-stage artifacts.
No back-propagation.
--------------------------------------------------
SCOPE
--------------------------------------------------

Process all directives in:

backtest_directives/active/

Sequentially using:

python tools/run_pipeline.py --all

run_pipeline.py is the sole authorized execution entry point.
Direct invocation of stage scripts is prohibited.

--------------------------------------------------
EXECUTION FLOW (NON-BYPASSABLE)
--------------------------------------------------

1. Preflight
2. Stage-1 (RESEARCH data only)
3. Stage-1.5 (if declared)
4. Stage-2
5. Stage-3
6. Stage-4 (if applicable)

Abort immediately on any failure.

--------------------------------------------------
ARTIFACT & INTEGRITY RULES
--------------------------------------------------

Each run must produce required artifacts.
If any artifact is missing → abort.

Do not:
- Recompute completed runs
- Modify historical artifacts
- Mutate strategy artifacts from portfolio layer
- Delete manually
- Continue after failure
